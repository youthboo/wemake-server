services:
  - type: web
    name: wemake-server
    runtime: go
    rootDir: tryly-server
    plan: free
    region: singapore
    buildCommand: go build -tags netgo -ldflags '-s -w' -o app ./cmd/app
    startCommand: ./app
    includeFiles:
      - migration
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: ENV
        value: production
      - key: PORT
        value: "3000"
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        sync: false
      - key: SMTP_HOST
        value: smtp.gmail.com
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: SMTP_FROM
        sync: false
      # Optional dev-only email relay. Use MAIL_PROVIDER=relay and point
      # MAIL_RELAY_URL at an ngrok-exposed local backend /internal/mail/send.
      - key: MAIL_PROVIDER
        sync: false
      - key: MAIL_RELAY_URL
        sync: false
      - key: MAIL_RELAY_TOKEN
        sync: false
      - key: PUBLIC_WEB_URL
        sync: false
      # Set in dashboard to https://<your-service>.onrender.com (no trailing slash).
      - key: PUBLIC_BASE_URL
        sync: false
      # Option A: single URL from Cloudinary dashboard (API Key Settings → CLOUDINARY_URL)
      - key: CLOUDINARY_URL
        sync: false
      # Option B: or set all three (leave CLOUDINARY_URL empty)
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      # Set in Render Dashboard (Neon / external Postgres). Do not commit credentials to git.
      - key: DATABASE_URL
        sync: false
